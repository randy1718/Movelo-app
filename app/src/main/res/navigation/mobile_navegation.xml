<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_host_fragment"
    app:startDestination="@+id/navigation_home">

    <fragment
        android:id="@+id/navigation_home"
        android:label="home page"
        tools:layout="@layout/main_page" >
        <action
            android:id="@+id/action_navigation_home_to_navigation_arboles"
            app:destination="@id/navigation_arboles" />
    </fragment>

    <fragment
        android:id="@+id/navigation_arboles"
        android:label="Tree market"
        tools:layout="@layout/tree_market" />

</navigation>